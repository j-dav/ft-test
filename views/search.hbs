<div>
  {{#if results}}
  {{#each results}}
  <a href={{this.location.uri}}>{{this.title.title}}</a>
  <p>{{this.editorial.subheading}}</p>
  {{/each}}
  {{else}}
  <h2>No results found for {{query}}</h2>
  {{/if}}
</div>